<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ナビゲーションバーのデモ</title>
<style>
  /* リンク間のスペースと区切り線のスタイル */
  .menu-item {
    border-bottom: 1px solid #eee; /* 薄いグレーの区切り線 */
    margin: 5px 0; /* 上下のマージンでリンク間のスペースを開ける */
    padding: 10px 0; /* リンクのパディングを調整 */
  }
  .menu-item:last-child {
    border-bottom: none; /* 最後のアイテムには区切り線を表示しない */
  }
  
  /* リンクにホバーした時の影 */
  .menu-item:hover {
    box-shadow: 0 2px 5px rgba(0,0,0,0.2);
    transition: box-shadow 0.3s ease-in-out;
  }

  .dropdown-content {
    z-index: 1000; /* 高い値を設定して前面に表示 */
  }
</style>
</head>
<body>

<div class="navbar bg-base-100">
  <div class="dropdown">
    <div tabindex="0" class="btn btn-square btn-ghost">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="inline-block w-6 h-6 stroke-current">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
    </div>
    <ul tabindex="0" class="menu menu-compact dropdown-content mt-3 p-2 shadow bg-base-100 rounded-box w-52">
      <%= link_to t('senryus.new.title'), new_senryu_path, class: 'menu-item' %>
      <%= link_to t('senryus.index.title'), senryus_path, class: 'menu-item' %>
      <%= link_to t('senryus.favorites.title'), favorites_senryus_path, class: 'menu-item' %>
      <%= link_to t('senryus.ranking.title'), senryus_ranking_path, class: 'menu-item' %>
    </ul>
  </div>

  <div class="flex-1">
    <%= link_to root_path, class: 'btn btn-ghost text-xl link' do %>
      <%= image_tag 'goshichide.png', class: "h-12", size: "70x70" %>
    <% end %>
  </div>

  <!-- ユーザーアイコンのドロップダウンメニュー -->
  <div class="dropdown dropdown-end">
    <div tabindex="0" role="button" class="btn btn-ghost btn-circle icon">
      <div class="w-10 rounded-full">
        <%= image_tag(current_user.icon_url, alt: "user icon", class: "w-10 rounded-full") %>
      </div>
    </div>
    <ul tabindex="0" class="mt-3 z-[1] p-2 shadow menu menu-sm dropdown-content bg-base-100 rounded-box w-52">
      <%= link_to (t 'profiles.show.title'), profile_path, class: 'menu-item' %>
      <%= link_to (t 'senryus.user_senryus.title'), user_senryus_senryus_path, class: 'menu-item' %>
      <%= link_to (t 'defaults.logout'), logout_path, data: { turbo_method: :delete }, class: 'menu-item' %>
    </ul>
  </div>
</div>

</body>
</html>
